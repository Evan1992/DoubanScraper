# DoubanScraper

Image crawling microservice built with FastAPI + Python 3.9.

## Run locally

```bash
/usr/local/bin/python3.9 -m uvicorn main:app --reload
```

Server runs at http://127.0.0.1:8000

## Install dependencies

```bash
pip3.9 install -r requirements.txt
```
